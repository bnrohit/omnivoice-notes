{
  "name": "omnivoice-notes",
  "version": "1.0.0",
  "private": true,
  "description": "Cross-device local-first voice recording, timestamped notes, transcription and AI summaries.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server.mjs",
    "dev": "node --watch server.mjs",
    "check": "node --check server.mjs && node --check public/app.js && node --check public/sw.js"
  }
}
